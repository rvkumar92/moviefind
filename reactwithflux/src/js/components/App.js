var React = require('react'),
    AppAction = require('../actions/AppAction'),
    AppStore = require('../store/AppStore');

var App = React.createClass({
    render(){
        return(
            <div>
                MovieFinder MYApp
            </div>
        )
    }
});

module.exports = App;